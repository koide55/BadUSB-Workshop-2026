# ジグソー資料3 ── マウスなしで Web ブラウザを操作する

- **位置づけ**: SECCON BadUSB ハンズオンの導入(ジグソー法・資料3/3)
- **ねらい**: マウスやトラックパッドが使えなくても、キーボードだけで Web ブラウジング(検索・フォーム操作)ができることを体感する
- **進め方**: 自分の OS の編に取り組み、[ワークシート](jigsaw-worksheet.md)に発見を書き留める

---

## この資料の論理(何のためにやるか)

ここで確かめたいのは、次の事実です。

> **ブラウザ上の操作 ── リンク移動・入力・ボタン押下 ── も、キーボードだけで完結できる。**

<kbd>Tab</kbd> でフォーカスを送り、<kbd>Enter</kbd> で押す。この繰り返しで、
サインイン・検索・カート投入まで、マウスなしでたどり着けます。

これが [BadUSB 本編](seccon-ws-badusb-rp2040.md)にどうつながるか。
資料1(OS 操作)・資料2(プログラム実行)に続き、資料3は
**「Web サービス上の操作すらキー入力だけで自動化できる」**ことを示します。
BadUSB は <kbd>Tab</kbd> と <kbd>Enter</kbd> の連打を人間より速く・正確に送れるため、
**ブラウザに保存されたセッションを使った操作**まで射程に入り得る、という理解に到達します。

だからこそ本編の防御パート(§8)で、
**離席時の画面ロック**と**ログイン状態の管理**が繰り返し強調されます。

## 倫理と前提

> - すべて **自分の PC(または許可された演習端末)・自分のアカウント**の上で行う。
> - **カートに入れるところまで**が演習範囲。**実際の購入・注文はしない**(本当に買わなくてよいです!)。
> - 他人のアカウント・共有端末に残ったログイン状態を操作しない。
> - 演習後、テスト用にカートへ入れた商品は元に戻しておく。

---

## 【Windows 編】

マウスやトラックパッドが壊れてしまっても、Web ブラウジングはできます。
Amazon でマウスを発注する体で試してみましょう(**カートまで。実際には注文しません**)。

### 1. ブラウザを立ち上げる

ここでは Google Chrome を使います(あらかじめインストールしておいてください)。
<kbd>Windows</kbd> キーを押すと画面左下の検索欄に入力できるようになります。
`Chrome` と入力して <kbd>Enter</kbd> で Chrome を起動します。

<kbd>Ctrl</kbd>+<kbd>L</kbd> で URL を入力できるので、`https://www.amazon.co.jp/` を入力して開きます。

### 2. キーボードだけでページを操作する

- <kbd>Tab</kbd> を何度も押すと、ページ内をフォーカス枠が移動していくのが分かります。
- <kbd>Shift</kbd>+<kbd>Tab</kbd> で逆順に移動します。
- クリックしたい項目が選択された状態で <kbd>Enter</kbd> を押すと、その部分をクリックしたことになります。

サインインしたり、商品を検索したりしてみましょう。
何回 <kbd>Tab</kbd> を押すとどこに枠が来るのか、ある程度数えておくと楽です。

### 3. マウス(商品)をカートに入れる

<kbd>Tab</kbd> / <kbd>Shift</kbd>+<kbd>Tab</kbd> / <kbd>Enter</kbd> を駆使して、
欲しいマウスのページにたどり着きましょう。欲しいものをカートに入れて、レジに進んでみましょう。

> **(本当に買わなくてよいですよ!)** 注文確定の一歩手前で止めます。

---

## 【Mac 編】

マウスやトラックパッドが壊れてしまっても、Web ブラウジングはできます。
Amazon でマウスを発注する体で試してみましょう(**カートまで。実際には注文しません**)。

### 1. ブラウザを立ち上げる

ここでは Google Chrome を使います(あらかじめインストールしておいてください)。
「マウスやトラックパッドが壊れた」という想定ですが、完全に壊れたわけではなく、
なんとか Chrome は起動できたことにしましょう。

<kbd>⌘</kbd>+<kbd>L</kbd> で URL を入力できるので、`https://www.amazon.co.jp/` を入力して開きます。

> **講師メモ**: Safari でも <kbd>⌘</kbd>+<kbd>L</kbd> で URL バーに移動できる。
> リンク間を <kbd>Tab</kbd> 移動させるには、Safari の環境設定でフルキーボードアクセスの有効化が要る場合がある。

### 2. キーボードだけでページを操作する

- <kbd>Tab</kbd> を何度も押すと、ページ内をフォーカス枠が移動します。
- <kbd>Shift</kbd>+<kbd>Tab</kbd> で逆順に移動します。
- クリックしたい項目が選択された状態で <kbd>Enter</kbd> を押すと、その部分をクリックしたことになります。

サインインしたり、商品を検索したりしてみましょう。
何回 <kbd>Tab</kbd> を押すとどこに枠が来るのか、ある程度数えておくと楽です。

### 3. マウス(商品)をカートに入れる

<kbd>Tab</kbd> / <kbd>Shift</kbd>+<kbd>Tab</kbd> / <kbd>Enter</kbd> を駆使して、
欲しいマウスのページにたどり着き、カートに入れてレジに進んでみましょう。

> **(本当に買わなくてよいですよ!)** 注文確定の一歩手前で止めます。

---

## 振り返り

これらを踏まえて、以下を調べてみましょう。書き留めておいてください。

1. Web ブラウザをキーボードで操作するとき、**できなさそうなこと**はありますか?
   いろいろ試したり調べたりして、できたこと・できなかったことを書き留めましょう。
2. 今回はキーボードでブラウザを操作しましたが、**もっと他にブラウザを操作する方法**はないでしょうか?
   試したり調べたりしながら、できそうな方法を探して書き留めましょう。

> **問い**: <kbd>Tab</kbd> と <kbd>Enter</kbd> の連打で買い物まで届くなら、
> ログイン済みのブラウザが開いた端末を離席中に触られたら何が起きるか?
> ── 本編の防御パート(画面ロック・ログイン状態管理)へのつながりです。
