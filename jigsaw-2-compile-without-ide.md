# ジグソー資料2 ── 開発環境なしでプログラムを動かす

- **位置づけ**: SECCON BadUSB ハンズオンの導入(ジグソー法・資料2/3)
- **ねらい**: 特別なインストールをしなくても、素の OS だけでプログラムを書いて実行できることを体感する
- **進め方**: 自分の OS の編に取り組み、[ワークシート](jigsaw-worksheet.md)に発見を書き留める

---

## この資料の論理(何のためにやるか)

ここで確かめたいのは、次の事実です。

> **買ってきたばかりの素の PC にも、プログラムを実行する手段が最初から備わっている。**

Windows なら Python、Mac ならスクリプト言語(Ruby や Python)を使えば、
Visual Studio や Xcode のような **大掛かりな開発環境を入れなくても**、
ターミナルに数行打ち込むだけでプログラムを書いて実行できてしまう。

これが [BadUSB 本編](seccon-ws-badusb-rp2040.md)にどうつながるか。
BadUSB が送れるのは **キー入力だけ**です。
しかし「素の OS にプログラム実行環境が揃っている」なら、
キー入力でターミナルを開き、ソースを打ち込み、実行させる ── という一連が **キーボードだけで完結**します。
資料1(キーボードだけで操作)と資料2(素の OS でプログラム実行)を合わせると、
**HID キーボード一つで任意コードの実行に至り得る**という BadUSB の脅威像が組み上がります。

> **重要**: この演習で書くのは **無害な "Hello World" だけ**です。
> 「では悪意あるコードを流し込めば…」という発想の実演はしません。
> 学ぶのは **仕組みの理解**であって、攻撃コードの作成ではありません。

## 倫理と前提

> - すべて **自分の PC(または許可された演習端末)** の上で行う。
> - 作成するのは画面に文字を出すだけの無害なプログラムに限る。
> - ネットワーク送信・ファイル削除・設定変更などを行うコードは書かない・試さない。

---

## 【Windows 編】

Visual Studio 等の開発環境をインストールしていなくても、
**Python** が入っていれば、コマンドプロンプトに数行打ち込むだけでプログラムを書いて実行できます。
下記を試してみましょう。

### 1. コマンドプロンプトを起動する

<kbd>Windows</kbd> キーを押すと画面左下の検索欄に文字を入力できるようになります。
`cmd` とタイプして <kbd>Enter</kbd> を押すと、コマンドプロンプトを起動できます。

まず Python が使えるか確認します。

```text
python --version
```

バージョン(例: `Python 3.12.x`)が表示されれば OK です。

### 2. ソースコードを入力し、ファイルに保存する

一字一句間違えないように、コマンドプロンプトに以下のようにタイプしてください。
難しいようなら、メモ帳に入力しておいてコピー・ペーストしましょう。

```text
copy con hello.py
```

続けて、ソースコードを入力します。

```python
print("Hello World!")
print("Press Enter to exit.")
input()
```

入力し終えたら <kbd>Ctrl</kbd>+<kbd>Z</kbd> を押し、<kbd>Enter</kbd> を押してファイルを保存します。

### 3. 実行する

```text
python hello.py
```

`Hello World!` と表示されれば成功です。Python で何ができるか調べて、いろいろ改造して試してみよう(画面表示の範囲で)。

> **講師メモ(現行環境向けの補足)**
> 元資料は Windows 標準の C# コンパイラ(`C:\Windows\Microsoft.NET\Framework\v3.5\csc.exe`)を使っていたが、
> このパスは古い .NET Framework 前提で、現行 Windows には存在しないことが多い。本資料では **Python に置き換えている**。
> - Python は素の Windows には既定で入っていない。**会場の PC に事前導入**しておくか、`python.org` / Microsoft Store から入れる。
> - Windows 10/11 には `python` の実行エイリアスがあり、未導入だと Store が開く。事前導入で回避する。
> - 「重い IDE を入れなくても、簡単な実行環境さえあればコードが動く」という論点が伝われば、
>   PowerShell(`Add-Type` 等)や他の手段に読み替えても構わない。

---

## 【Mac 編】

Xcode 等の開発環境をインストールしていなくても、
Mac には実は **さまざまなスクリプト言語**が標準で搭載されています。
買ってきたばかりのコンピュータでもプログラムを書いて実行できます。下記を試してみましょう。

### 1. ターミナルを起動する

Launchpad → その他 → ターミナル をクリックして、Terminal を起動します。
(Spotlight <kbd>⌘</kbd>+<kbd>Space</kbd> で `Terminal` と打つのも早い)

### 2. ソースコードを入力する

一字一句間違えないように、以下のようにタイプしてください(説明の文はタイプしなくてよいです)。

```text
cat > hello.rb
```

続けて、1行だけの Ruby プログラムを入力します。

```ruby
puts "hello"
```

入力し終えたら <kbd>Ctrl</kbd>+<kbd>D</kbd> を押してファイルを保存します。

### 3. 実行する

```text
ruby hello.rb
```

Ruby 言語で何ができるか調べて、いろいろ改造して試してみましょう(画面表示の範囲で)。

> **講師メモ**: 近年の macOS では標準 Ruby が将来的に整理される可能性がある。
> Ruby が無い場合は `python3` でも同じ論点を示せる(例: `cat > hello.py` → `print("hello")` → `python3 hello.py`)。

---

## 振り返り

これらを踏まえて、この方法でどんなことができそうかまとめてみよう。
気づいたことを [ワークシート](jigsaw-worksheet.md) に書き留めておいてください。

> **問い**: 「キーボードで操作できる(資料1)」+「素の OS でプログラムを実行できる(資料2)」。
> この二つが組み合わさると、USB を挿しただけのデバイスに何ができてしまうか?
> ── これが本編で作る BadUSB の脅威モデルです。
